# Оптимизация bcrypt и Argon2 с помощью асинхронности и корутин

## Проблема

Bcrypt и Argon2 - очень медленные алгоритмы хэширования (специально разработаны для замедления брутфорса). При синхронной проверке каждый кандидат проверяется последовательно, что очень медленно.

## Решение

Реализована асинхронная параллельная обработка с батчингом для ускорения проверки bcrypt и Argon2.

## Внесенные улучшения

### 1. Создан AsyncHashChecker

Новый класс `AsyncHashChecker` для асинхронной проверки медленных алгоритмов:

- **Батчинг**: Собирает несколько кандидатов и проверяет их параллельно
- **Параллелизм**: Использует Semaphore для контроля количества одновременных проверок
- **Асинхронность**: Все проверки выполняются через корутины с `async/await`

### 2. Параллельная обработка батчей

Вместо последовательной проверки:
```kotlin
// Старый способ (медленно)
for (candidate in candidates) {
    if (checker.check(candidate)) return candidate
}
```

Новый способ с параллельной обработкой:
```kotlin
// Новый способ (быстро)
val results = asyncChecker.checkBatchWithLimit(batch)
// Все кандидаты проверяются параллельно!
```

### 3. Оптимизация для разных алгоритмов

- **Bcrypt**: Размер батча = 15, параллелизм = threadCount * 1.5
- **Argon2**: Размер батча = 10, параллелизм = threadCount * 1.5
- **MD5/SHA-1**: Остаются без изменений (и так быстрые)

### 4. Интеграция в BruteForceService

- Автоматическое определение медленных алгоритмов
- Создание AsyncHashChecker только для bcrypt/Argon2
- Батчинг кандидатов перед проверкой
- Параллельная обработка батчей с ограничением через Semaphore

## Технические детали

### AsyncHashChecker

```kotlin
class AsyncHashChecker(
    private val checker: HashChecker,
    private val batchSize: Int = 10,
    private val parallelism: Int = Runtime.getRuntime().availableProcessors()
) {
    suspend fun checkBatchWithLimit(candidates: List<String>): List<Pair<String, Boolean>>
}
```

**Особенности:**
- Использует `Semaphore` для контроля параллелизма
- Все проверки выполняются через `async(Dispatchers.Default)`
- Результаты собираются через `awaitAll()`

### Батчинг в BruteForceService

1. **Сбор кандидатов**: Собираем батч из 10-15 кандидатов
2. **Параллельная проверка**: Все кандидаты проверяются одновременно
3. **Обработка результатов**: Проверяем результаты и останавливаемся при нахождении

## Производительность

### Ожидаемые улучшения:

- **Параллелизм**: Вместо 1 проверки за раз - до `threadCount * 1.5` проверок одновременно
- **Использование CPU**: Все ядра CPU используются для проверки
- **Скорость**: Ускорение пропорционально количеству потоков (обычно 4-8x на 4-8 ядерном CPU)

### Пример:

**До оптимизации:**
- 1 проверка bcrypt = ~100ms
- 10 проверок последовательно = 1000ms

**После оптимизации (8 потоков):**
- 10 проверок параллельно = ~125ms (8x быстрее!)

## Использование

Оптимизация применяется автоматически для bcrypt и Argon2. Никаких дополнительных параметров не требуется.

При запуске вы увидите:
```
🚀 Using async batch processing with 8 parallel workers for acceleration...
```

## Заключение

Асинхронная обработка с батчингом значительно ускоряет проверку bcrypt и Argon2 за счет:
- Параллельной обработки нескольких кандидатов
- Эффективного использования всех ядер CPU
- Минимизации времени простоя потоков

Это критически важно для медленных алгоритмов, где каждая проверка занимает много времени.

